import React from "react";

import AddItemForm from "../models/add-item-form/add-item-form";

const AddItemPage = (): JSX.Element => {
    return <AddItemForm onSubmit={() => null} onClose={() => null} />;
};

export default AddItemPage;
